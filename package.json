{
  "name": "drunkrobot",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "@fontsource/anton": "^5.1.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "pagefind": "^1.5.2",
    "vitest": "^3.0.0"
  }
}
